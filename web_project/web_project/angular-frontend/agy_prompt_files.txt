# ROL: Desarrollador Angular Senior

El proyecto Angular ya existe en:
/mnt/c/Users/juanp/Documents/Proyectos/proyecto-final-web/web_project/web_project/angular-frontend/

Ya tiene la estructura base (angular.json, package.json, src/main.ts, etc).

## TU TAREA: Crear todos los archivos TypeScript del frontend

Crea cada archivo con su codigo completo. Trabaja en orden.

### 1. MODELS (src/app/models/)

Crea la carpeta models/ y estos archivos:

**user.model.ts:**
```typescript
export interface User {
  userId: number;
  name: string;
  email: string;
  passwordHash?: string;
  createdAt: string;
}
```

**dataset.model.ts:**
```typescript
export interface Dataset {
  datasetId: number;
  name: string;
  description: string;
  datasetType: string;
  numSamples: number;
  numFeatures: number;
  targetVariable: string;
  createdAt: string;
}
```

**ml-model.model.ts:**
```typescript
export interface MlModel {
  modelId: number;
  name: string;
  category: string;
  description: string;
}
```

**uploaded-file.model.ts:**
```typescript
export interface UploadedFile {
  fileId: number;
  fileName: string;
  storagePath: string;
  fileSize: number;
  uploadedAt: string;
  userId: number;
}
```

**experiment.model.ts:**
```typescript
export interface Experiment {
  experimentId?: number;
  userId: number;
  modelId: number;
  datasetId: number;
  parameters: string;
  accuracy?: number;
  mse?: number;
  r2Score?: number;
  status: string;
  createdAt?: string;
}
```

**ml-result.model.ts:**
```typescript
export interface MlResult {
  predictions: number[];
  metrics: Record<string, number>;
}
```

**email.model.ts:**
```typescript
export interface Email {
  to: string;
  subject: string;
  body: string;
}
```

### 2. ENVIRONMENT

Modifica src/environments/environment.ts (crea la carpeta si no existe):
```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};
```

Y environment.development.ts igual.

### 3. SERVICIOS (src/app/services/)

**api.service.ts** — Servicio base:
```typescript
import { Injectable } from '@angular/core';
import { HttpClient, HttpErrorResponse } from '@angular/common/http';
import { Observable, throwError } from 'rxjs';
import { catchError } from 'rxjs/operators';
import { environment } from '../../environments/environment';

@Injectable({ providedIn: 'root' })
export class ApiService {
  protected apiUrl = environment.apiUrl;
  constructor(protected http: HttpClient) {}
  protected handleError(error: HttpErrorResponse) {
    console.error('API Error:', error);
    return throwError(() => new Error(error.message || 'Error del servidor'));
  }
}
```

**user.service.ts** — Extiende ApiService. Metodos: getAll(), getById(id), create(user), update(id, user), delete(id), login(email, password).

**dataset.service.ts** — CRUD + getByType(type).

**model.service.ts** — CRUD.

**file.service.ts** — upload(formData: FormData), getAll(), getById(id), delete(id).

**experiment.service.ts** — CRUD + getByUserId, getByModelId.

**ml.service.ts** — runLinear(data), runLogistic(data), runKnn(data). POST al backend.

**email.service.ts** — sendEmail(email). POST.

**pdf.service.ts** — getExperimentPdf(id), getDatasetPdf(id). responseType: 'blob'.

### 4. COMPONENTES COMPARTIDOS (src/app/shared/)

Cada uno en su carpeta: nombre.component.ts, nombre.component.html, nombre.component.css.

**navbar.component.ts** — standalone, imports: [RouterLink, RouterLinkActive].
Template: nav con fondo #1a237e, links a: Inicio, Datasets, Modelos, Experimentos, Archivos, ML.

**footer.component.ts** — standalone. Template: footer con texto "IPN - ESCOM | ML Lab | 2026".

**loading-spinner.component.ts** — @Input() isLoading: boolean. Muestra spinner CSS.

**data-table.component.ts** — @Input() columns: string[], @Input() data: any[], @Input() title: string. Tabla HTML simple.

**empty-state.component.ts** — @Input() message: string. Muestra mensaje.

**error-message.component.ts** — @Input() message: string, @Output() retry = new EventEmitter().

### 5. PAGINAS (src/app/pages/)

Cada pagina: standalone component con su template y estilos basicos. Importa los componentes shared que necesite. Usa el nuevo control flow (@if, @for).

Crea estas paginas (nombre de carpeta = nombre del componente):

- home/ — Titulo, descripcion del proyecto, tarjetas de acceso rapido con routerLink
- login/ — Formulario email+password, boton Iniciar Sesion, llama userService.login()
- dashboard/ — Tarjetas con estadisticas (total experimentos, datasets, modelos)
- users/user-list/ — Tabla de usuarios con DataTableComponent
- users/user-form/ — Formulario crear/editar usuario con template-driven forms
- users/user-detail/ — Vista detalle de usuario
- datasets/dataset-list/ — Tabla con filtro por tipo
- datasets/dataset-form/ — Formulario crear/editar
- datasets/dataset-detail/ — Detalle + boton descargar PDF
- models-ml/model-list/ — Catalogo de modelos
- models-ml/model-form/ — Formulario
- models-ml/model-detail/ — Detalle
- experiments/experiment-list/ — Tabla con filtros
- experiments/experiment-form/ — Formulario: seleccionar dataset (dropdown), modelo (dropdown), parametros (textarea)
- experiments/experiment-detail/ — Metricas (Accuracy, MSE, R2), botones PDF y Email
- experiments/experiment-run/ — Pantalla "Ejecutando experimento..."
- files/file-upload/ — Input type=file, FormData, boton Subir
- files/file-list/ — Tabla de archivos
- ml/ml-linear/ — Formulario simple para regresion lineal
- ml/ml-logistic/ — Formulario para regresion logistica
- ml/ml-knn/ — Formulario para KNN

### 6. APP.ROUTES.TS

Sobreescribe src/app/app.routes.ts con todas las rutas.
Importa todos los componentes de paginas.

### 7. APP.COMPONENT.* 

app.component.ts: standalone, imports: [RouterOutlet, NavbarComponent, FooterComponent]
app.component.html: app-navbar + router-outlet + app-footer
app.component.css: estilos globales basicos, margenes, fuente

### 8. APP.CONFIG.TS

Asegura que app.config.ts tenga:
```typescript
import { ApplicationConfig, provideZoneChangeDetection } from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideHttpClient, withFetch } from '@angular/common/http';
import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideZoneChangeDetection({ eventCoalescing: true }),
    provideRouter(routes),
    provideHttpClient(withFetch())
  ]
};
```

### REGLAS:
- SIN Angular Material, SIN emojis, SIN dark themes
- Diseño simple, funcional, CSS plano
- Colores: fondo blanco, texto negro, navbar azul (#1a237e), botones azules
- Comentarios en español
- Todos los componentes standalone con imports en el decorador
- Usa @if/@for (Angular 18+ control flow)
- Formularios: FormsModule (template-driven)
- Servicios extienden ApiService con providedIn: 'root'
- NO crees archivos .spec.ts
- Cada componente maneja estado loading/error/success

Trabaja archivo por archivo. Para cada uno, escribe el codigo COMPLETO. No hagas resumenes.
